"""Prompt name for the batched field-extraction LLM call.

The prompt text itself lives in ``PROMPT.md`` §Agent 13 and is loaded through
the prompt registry, so this agent shares the versioning, provenance notes and
snapshot tests that cover the other twelve.
"""
from __future__ import annotations

PROMPT_NAME = "doc_intelligence"
